#!/bin/bash

# 🍆💦 KDE MODE SWITCHER 💋🔥
# Switch between Degenerate, Horny, and Normal KDE themes

MODE=$1
CONFIG_DIR="${HOME}/.config"
KDE_DIR="${HOME}/.local/share"
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SOURCE_DIR="$(dirname "$SCRIPT_DIR")"